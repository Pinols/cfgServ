spring:
  application:
    name: microserviceone
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8081

print:
  me: Devprofile

eureka:
  instance:
    preferIpAddress: true
