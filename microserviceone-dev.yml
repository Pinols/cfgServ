spring:
  application:
    name: microserviceone
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  main:
    allow-bean-definition-overriding: true

server:
  port: 8081

print:
  me: Dev profile
